export type FiltersPayload  = {
    courseCode: string, 
    startSemester: string, 
    endSemester: string, 
    className: string, 
    campus: string, 
    approved: boolean, 
    toApprove: Boolean, 
    studentName: string
}